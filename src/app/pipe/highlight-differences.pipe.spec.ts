import { HighlightDifferencesPipe } from './highlight-differences.pipe';

describe('HighlightDifferencesPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightDifferencesPipe();
    expect(pipe).toBeTruthy();
  });
});
